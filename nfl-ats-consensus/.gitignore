__pycache__/
*.pyc
.venv/

# Generated each run from the locked line plus live nflverse data.
data/dossiers/
# Model submissions and grades. Remove these two lines if you want the full
# audit trail committed to git — recommended once you are running for real.
data/picks/
data/results/

# Secrets. Never commit a key — see README "Where the API key goes".
.env
.env.*
!.env.example
*.key
*.pem
secrets.json
