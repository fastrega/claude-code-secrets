# Copy to .env and fill in. .env is gitignored and must never be committed.
#
#   cp .env.example .env
#   chmod 600 .env
#   $EDITOR .env
#
# Get a key at https://openrouter.ai/keys — one key covers the whole field.
OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
